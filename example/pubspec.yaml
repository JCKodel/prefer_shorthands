name: example
description: A sample command-line application with basic argument parsing.
version: 0.0.1

environment:
  sdk: ^3.10.0

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
